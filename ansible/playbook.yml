---
# StudyAI - Server Configuration Playbook

- name: Configure StudyAI production server
  hosts: webservers
  become: true

  vars_files:
    - group_vars/all.yml
    - group_vars/vault.yml

  roles:
    - common
    - docker
    - nginx
    - app
